    {% if postListHeadline %}
    <h2 class="post-rec">
      {{ postListHeadline }}
    </h2>
    {% endif %}
     <section>
      <div class="post-content sm:rounded-2xl">
        {% for item in postListItems %}
        {% set currentPost = post.data.post %}
					<article class="m-auto mb-10 overflow-hidden">
						<div class="no-underline hover:no-underline flex flex-wrap">
              {% if item.data.imageURL %}
              <div class="mx-auto overflow-hidden md:flex-1 md:pr-4 sm:w-full md:w-6/12">
              <img class="lazy m-0 w-full max-w-full h-auto rounded-md" src="{{ item.data.imageURL}}" alt="{{ item.data.imageAlt }}">
              </div>
            {% endif %}
            <div class="flex-1">
            <time class="serif text-sm text-white pl-2 pr-2 pt-1 pb-1 rounded-2xl bg-purple-500" datetime="{{ item.data.date }}"> 🕒 {{ item.data.date | dateFilter }}</time>
            <h2 class="mt-4"><a class="border-none text-xl md:text-3xl" href="{{ item.url }}">{{ item.data.title }}</a></h2>
              <p  class="text-gray-800 font-sans sm:text-xl md:text-2xl mb-5">{{ item.data.description }}</p>
            <a class="inline-block mt-6 rounded-xl px-4 py-2 bg-teal-200 font-bold text-slate-600 text-center text-sm hover:bg-yellow-100 hover:text-slate-600 hover:border-yellow-400 hover:border-2"href="{{ item.url }}">Read 👉</a>
           </div>
         </div>
      </article>
        {% endfor %}
        </div>
     </section>

