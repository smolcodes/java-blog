    <style>
    header { background-image: url("{{ pageHeaderImage }}");
    }
    </style>
    <header id="header" class="max-w-full mb-5 md:mb-20 py-16 bg-orange-300">
    <div class="container mx-auto break-normal text-gray-50 p-10">
    <h1 class="text-slate-600 font-serif font-extrabold text-4xl md:text-6xl">
      <span class="bg-orange-300">{{ pageHeaderTitle }}</span>
    </h1>
    {% if pageHeaderSummary %}
       <p class="leading-loose my-5 md:my-10 text-2xl">
         {{ pageHeaderSummary | safe }}
      </p>
    {% endif %}
    </div>
</header>
