{% extends "layouts/base.njk" %}
 {% set pageHeaderTitle = title %}
 {% set pageHeaderImage = imageURL %}
 {% set pageHeaderSummary = content %}
{% block content %}
      <section class="flex flex-wrap justify-between pt-12 -mx-6 ">
         {% for item in collections.books %}
        <article class="w-full md:w-1/2 p-6 flex flex-col flex-grow flex-shrink">
        <div class="flex-1 bg-pink-50 rounded-t rounded-b overflow-hidden shadow-md hover:shadow-2xl border-4 border-transparent hover:border-pink-500 pb-5">
          <div class="flex-grow px-10 mt-8">  
           <h2 class="w-full"><a class="text-4xl font-bold font-serif text-red-400" href="{{ item.url }}">{{ item.data.title }}</a></h2>
          </div>
 </div>
       <div class="pt-12 pb-12">
           <p  class="text-gray-800 font-serif text-base mb-5"> <b>Author: </b>{{ item.data.author }} <b>Genre</b> {{ item.data.genre}}</p>
          <a class="bg-yellow-100 p-2 hover:bg-yellow-300 mb-4"href="{{ item.url }}">Read 👉</a>
           </div>
 </article>
        {% endfor %}
        </section>
{% endblock %}

