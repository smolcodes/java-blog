{% extends "layouts/base.njk" %}
 {% set pageHeaderTitle = title %}
 {% set pageHeaderImage = imageURL %}
 {% set pageHeaderSummary = content %}
 {% set postListItems = pagination.items %}
{% block content %}
      <section>
         {% for item in postListItems %}
       <article class="p-2 md:p-4 border-b-2 border-gray-500">
		<div class="p-2 md:p-8 hover:bg-pink-100 border-2 border-gray-500 bg-gray-100 rounded-t rounded-b overflow-hidden hover:shadow-md hover:shadow-2xl border-4 border-transparent hover:border-pink-500 pb-5">
           <h2><a class="text-2xl md:text-4xl font-bold font-serif text-red-400" href="{{ item.url }}">{{ item.data.title }}</a></h2>
          </div>
       <div class="pt-12 pb-12">
           <p  class="text-gray-800 font-serif text-base mb-5"> <b>Author: </b>{{ item.data.author }} <b>Genre</b> {{ item.data.genre}}</p>
          <a class="bg-yellow-100 p-2 hover:bg-yellow-300 mb-4"href="{{ item.url }}">Read 👉</a>
           </div>
 </article>
        {% endfor %}
        </section>
           {% if not tag %}
     {% include "partials/pagination.njk" %}
    {% endif %}
{% endblock %}

